#include<iostream>
#include <vector>
#include<math.h>

using namespace std;

float EuclideanDistance(vector<int>p1,vector<int> p2);
vector<float> SwitchDistance(vector<vector<int>>box);
bool SwitchState(vector<float>distance);